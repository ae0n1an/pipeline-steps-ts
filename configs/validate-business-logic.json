{
  "files": [
    {
      "name": "usersVsResult",
      "csvPath": "{{steps.genUsersCsv.outputs.usersCsv_csvPath}}",
      "jsonPath": "{{steps.gpgDecryptResult.outputs.result_decryptedPath}}",
      "csvKeyField": "id",
      "jsonKeyField": "id",
      "rules": [
        { "type": "rowCountMatches" },
        { "type": "allCsvRowsHaveJsonMatch" }
      ]
    }
  ]
}
